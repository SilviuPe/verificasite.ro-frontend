import type {defaultDataPropsI} from "../../api.ts";

const Marketing = (props: defaultDataPropsI) => {

    const {data} = props;
    console.log(data);
    return (
        <></>
    )
}

export {Marketing};