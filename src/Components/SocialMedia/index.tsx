import type {defaultDataPropsI} from "../../api.ts";

const SocialMedia = (props: defaultDataPropsI) => {

    const {data} = props;
    console.log(data);
    return (
        <></>
    )
}

export {SocialMedia};