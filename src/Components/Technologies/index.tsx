import type {defaultDataPropsI} from "../../api.ts";

const Technologies = (props: defaultDataPropsI) => {

    const {data} = props;
    console.log(data);
    return (
        <></>
    )
}

export {Technologies};